{
  "name": "pixly-format-vue",
  "version": "3.0.0",
  "description": "PIXLY Format - Professional Image Converter (Vue.js)",
  "type": "module",
  "scripts": {
    "setup": "bash setup-bin.sh",
    "dev": "vite",
    "build": "vite build",
    "release": "bash ../shared/build.sh && mkdir -p bin && cp ../shared/bin/pixly-eagle-core bin/ && npm run build && echo '✅ Release package ready in dist/'",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}